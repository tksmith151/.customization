# Declare all dotfiles
declare -a dotfiles=("vimrc")

# Remove Old Files and Symlinks
for dotfile in "${dotfiles[@]}"
do
    echo "$dotfile"
    ln -sf ~/.dotfile/"$dotfile" ~/"$dotfile"
done

# Create New Symlinks